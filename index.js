/**
    let slider = document.querySelector('.project-slider');
    
    
    slider.addEventListener('click',()=>{
    slider.classList.add('paused')
    })
*/
// Example of listening to slide change event
